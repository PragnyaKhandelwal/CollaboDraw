# render.yaml — Render Blueprint for CollaboDraw (Docker)
services:
  - type: web
    name: collabodraw
    repo: https://github.com/PragnyaKhandelwal/CollaboDraw
    branch: main
    env: docker
    plan: free
    healthCheckPath: "/actuator/health"
    # Do NOT include secrets here. Set them in Render Dashboard.
