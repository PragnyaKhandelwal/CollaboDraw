# Copy this file to .env (which is ignored) and fill in real values.
# Cloud SQL instance identifier (needed for proxy)
CLOUD_SQL_INSTANCE=test-e470b:asia-south2:collabodraw

# Proxy listen host/port
DB_HOST=127.0.0.1
DB_PORT=3307

# Database schema name
DB_NAME=collaborative_workspace_db

# Credentials (DO NOT COMMIT REAL PASSWORDS)
DB_USER=your_db_user
DB_PASS=your_db_password

# Optional: Google service account JSON path (if not using gcloud ADC)
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/service-account.json
