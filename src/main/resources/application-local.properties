# Local / Proxy MySQL fallback profile
# Activate with: SPRING_PROFILES_ACTIVE=local
# Expects a running local proxy or MySQL on DB_HOST:DB_PORT.

spring.datasource.url=jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3307}/${DB_NAME:collaborative_workspace_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=${DB_USER:prag1704}
# For local only you MAY keep a default (remove for production hygiene)
spring.datasource.password=${DB_PASS:password}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
