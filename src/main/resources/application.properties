<<<<<<< HEAD
spring.application.name=collabodraw

############################################
# Database Configuration (Proxy-First Mode)
#
# You requested ALWAYS using the Cloud SQL Proxy and not the direct socket factory.
# Therefore we treat the app as a normal MySQL client connecting to host/port.
# Start proxy (or have one running) that forwards the Cloud SQL instance to a local port.
#
# Required environment variables:
#   DB_HOST (default 127.0.0.1)
#   DB_PORT (default 3307)
#   DB_NAME
#   DB_USER
#   DB_PASS
#
# CLOUD_SQL_INSTANCE is now informational ONLY for scripts; it does NOT change the URL.
# If you later want direct socket factory again, reintroduce the cloud SQL URL block.
#.\cloud-sql-proxy.exe --port 3307 test-e470b:asia-south2:collabodraw
# Example startup (PowerShell):
#   start /b "" C:\Users\you\cloud-sql-proxy.exe --port 3307 test-e470b:asia-south2:collabodraw
#   $env:DB_NAME="collaborative_workspace_db"
#   $env:DB_USER="prag1704"; $env:DB_PASS="***"
#   ./mvnw spring-boot:run
#
# Fail-fast tweaks: lowered connection-timeout & initialization-fail-timeout so errors show quickly.
############################################

db.name=${DB_NAME}
db.user=${DB_USER}
db.pass=${DB_PASS}
db.host=${DB_HOST:127.0.0.1}
db.port=${DB_PORT:3307}
cloud.sql.instance=${CLOUD_SQL_INSTANCE:}

# Single proxy-based JDBC URL (simplified, no SSL)
spring.datasource.url=jdbc:mysql://${db.host}:${db.port}/${db.name}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&connectTimeout=5000&socketTimeout=10000&tcpKeepAlive=true

spring.datasource.username=${db.user}
spring.datasource.password=${db.pass}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.initialization-fail-timeout=1
spring.datasource.hikari.validation-timeout=5000

# (Schema pre-provisioned in cloud DB; disable local init)
# spring.sql.init.mode=never
# spring.jpa.hibernate.ddl-auto=none

# Server port (optional override)
# server.port=8080

# OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=25211431577-theqgt5m2nllt7r96k1esd9b2qo36ism.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# Logging Configuration
logging.level.org.springframework.web=INFO
logging.level.org.springframework.boot.autoconfigure.web=INFO

# Static Resources Configuration
spring.web.resources.add-mappings=true

############################################
# Usage Summary
#   Proxy mode only: ensure cloud-sql-proxy running -> connect via DB_HOST:DB_PORT
#   CLOUD_SQL_INSTANCE optional (used by scripts for clarity)
#   If DB_PASS missing, startup should fail fast.
############################################
=======
spring.application.name=whiteboard
spring.datasource.url=jdbc:mysql://127.0.0.1:3307/collaborative_workspace_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=id
spring.datasource.password=pswrd

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.security.oauth2.client.registration.google.client-id= 
spring.security.oauth2.client.registration.google.client-secret= 

spring.security.oauth2.client.registration.google.scope=openid,profile,email
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot.autoconfigure.web=DEBUG
# Static resource configuration
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.cachecontrol.max-age=3600
spring.mvc.static-path-pattern=/js/**,/images/**

# Enable resource chain optimizations
spring.web.resources.chain.strategy.content.enabled=true
spring.web.resources.chain.strategy.content.paths=/**

>>>>>>> c52324823d0a0c2d7a2c828b7bfaa7a6ebe4d282
