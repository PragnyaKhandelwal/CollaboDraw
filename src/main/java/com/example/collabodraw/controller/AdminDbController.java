package com.example.collabodraw.controller;

// DB health and seed endpoints removed per request.
// This placeholder avoids compile errors if the file remains on disk.
final class AdminDbController { }
